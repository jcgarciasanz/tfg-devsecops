version: '3.8'

services:
  jenkins:
    image: jenkins/jenkins:lts
    container_name: jenkins-tfg
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - ../../jenkins-data:/var/jenkins_home
    restart: unless-stopped
